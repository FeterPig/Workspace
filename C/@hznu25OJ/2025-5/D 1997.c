#include <stdio.h>

void func() {}

int main()
{
    // arr[11]留空, 存放返回值
    int arr[11] = {0};
    while (scanf("%d %d %d %d %d %d %d %d %d %d", arr) != EOF)
    {
        /* code */
    }

    return 0;
}