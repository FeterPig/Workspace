#include <stdio.h>

// 表示n个盘子从c1位置移动到c3位置，借助c2。
void move(int n, char c1, char c2, char c3);
int main()
{
    int n;
    scanf("%d", &n);
    move(n, 'a', 'b', 'c');
}

void move(int n, char c1, char c2, char c3)
{
    /**
     * 程序主体
     */
    return 0;
}